[project]
name = "tongpin-hackathon-team-matcher"
version = "1.0.0"
description = "Flask application for finding hackathon teammates"
requires-python = ">=3.12"
dependencies = [
    "Flask>=3.1,<4",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
